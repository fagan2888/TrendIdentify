
# zigzag algorithm
# created by zhaoliyuan

import numpy as np
import pandas as pd
from optparse import OptionParser

class zigzag():

    def __init__(self, depth, backstep, deviation):
        self.depth = depth
        self.backstep = backstep
        self.deviation = deviation

    def handle():

        return signal


if __name__ == '__main__':
  
    opt = OptionParser()
    opt.add_option()
    opt.add_option()
    options,args = opt.parse_args()
    zigzag = zigzag(options.depth, options.backstep, options.deviation)
    zigzag.handle()
